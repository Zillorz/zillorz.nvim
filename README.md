My NvChad settings!

These are now mostly lazy-loaded, leading to incredibly fast startup times (<30ms on Linux, <100ms on Windows).

Little external setup needed
Cargo, Clang, tree-sitter-cli, fd-find, ripgrep, lazygit, git
